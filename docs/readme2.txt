-After downloading data relaunching the app can take a little bit (wait a min or so)
-repo manager does code reviews for merge requests, unless repo manager is submitting
	the merge request, in which case someone else on the team does the review